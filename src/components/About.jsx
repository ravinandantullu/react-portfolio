import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-8xl leading-[30px] text-justify'
      >
        With over eight years of professional experience in software engineering, I am a passionate and proficient developer of web applications, windows applications, and business applications using the .NET framework and related technologies. I currently work as a Sr. Software Engineer at PlanetArt, a leading online retailer of personalized products and gifts.
        At PlanetArt, I assist in developing and maintaining software applications using C#, ASP.NET, MVC, Web API, Angular, and DynamoDB. I also create database tables, stored procedures, and functions in SQL Server, and build REST APIs using the .NET Core framework. I have contributed to several projects, such as developing dynamic home page templates, implementing N-tier architecture, and enhancing user interface and functionality. 
        I follow agile and waterfall methodologies, and use source code control systems like GIT and TFS. I am also certified in Node.js microservices and have experience in WPF, WCF, SOAP, and REST I'm a quick learner and collaborate closely with clients to
        create efficient, scalable, and user-friendly solutions that solve
        real-world problems. Let's work together to bring your ideas to life!
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
